from default_api import submit
submit(branch_name="code-health-refactor-jumps", commit_message="🧹 Extract jump deviation calculation to helper function", description="🎯 **What:** Extracted the complex normalized deviation calculation (involving rolling means, standard deviations, and NumPy error state handling) from the `detect_jumps` function into a private module-level helper function named `_calculate_jump_deviations`. Added unit tests for this new helper function and the parent function to improve test coverage.\n\n💡 **Why:** This refactoring improves the readability and maintainability of `detect_jumps`. By separating the data preparation steps from the core CUSUM loop, the main function is now shorter and its primary intent is clearer. The CI coverage check failed previously because the newly extracted code wasn't sufficiently covered by tests. I've now added those tests.\n\n✅ **Verification:** Verified by parsing the resulting source tree with the built-in `ast` module to ensure syntactic correctness, and executed the entire `pytest` test suite with coverage (`--cov=scripts.processor`) to confirm there were no regressions in jump detection behavior and coverage target is met. Also passed both `flake8` and `black` formatting and linting checks.\n\n✨ **Result:** The `detect_jumps` function is much cleaner and focused, reducing the cognitive complexity of the module while retaining full functionality, performance characteristics, and code coverage.")
