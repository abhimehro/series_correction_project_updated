# https://docs.devin.ai/onboard-devin/environment-yaml
maintenance: |
  PYENV_VERSION=3.10.16 python -m pip install --upgrade pip
  PYENV_VERSION=3.10.16 python -m pip install -r scripts/requirements-dev.txt
  PYENV_VERSION=3.10.16 python -m pip install -e .
  PYENV_VERSION=3.10.16 python -m pip install --user ruff==0.15.8
knowledge:
  - name: build
    contents: |
      PYENV_VERSION=3.10.16 python setup.py --name
      PYENV_VERSION=3.10.16 python -m py_compile scripts/*.py
  - name: lint
    contents: |
      PYENV_VERSION=3.10.16 python -m ruff check scripts
      PYENV_VERSION=3.10.16 python -m black --check scripts
      PYENV_VERSION=3.10.16 python -m flake8 scripts/ --max-line-length=100
  - name: test
    contents: |
      PYENV_VERSION=3.10.16 python -m pytest scripts/tests/ -v
      Full suite passes (140 tests on Python 3.10 and 3.11).
  - name: python-version
    contents: |
      Use Python 3.10 or 3.11. Runtime dependencies (pandas, numpy, openpyxl) are pinned with exact versions in scripts/requirements.txt; dev/test/lint tools live in scripts/requirements-dev.txt. setup.py only installs the runtime set.
